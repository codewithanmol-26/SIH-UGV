# Vite only exposes variables prefixed with VITE_ to the browser. Copy this
# file to frontend/.env and adjust if your backend isn't on localhost:8000.

VITE_API_BASE=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000/ws/telemetry
